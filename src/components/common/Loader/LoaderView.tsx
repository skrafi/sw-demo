import * as React from 'react';
import { Loader, LoaderWrapper } from './Loader';

export const LoaderView = () => {
    return (
        <LoaderWrapper>
            <Loader />
        </LoaderWrapper>
    );
};
